import React from 'react';
import ReactDOM from 'react-dom/client';
import RouterApp from './components/Pages/Services/Router';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RouterApp/>
  </React.StrictMode>
)